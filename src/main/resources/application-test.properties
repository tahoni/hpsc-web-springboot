# STANDARD
## Database
spring.jpa.hibernate.ddl-auto=create-drop
spring.datasource.url=jdbc:h2:mem:testdb
# Hibernate generates the H2 schema directly from the entities on every test run;
# Flyway (MySQL-only migrations) is not involved in the test profile.
spring.flyway.enabled=false
